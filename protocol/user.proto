syntax = "proto3";

package user;

enum AcceptMessageFlags {
  RequestUserInfo = 0;
}

message Packet { AcceptMessage message = 1; }

message AcceptMessage { AcceptMessageFlags flags = 1; }

message User {
  string id = 1;
  string name = 2;
}